;; Copying pretty much everything from the Python version, including the below
;; Need to evaluate later and probably cleanup
;  see: http://legacy.python.org/dev/peps/pep-0440/#public-version-identifiers
(define VERSION "1.0.0")
